package com.powersi.switches.service;

import java.io.Serializable;

/**
 * 要素条件判断
 * 
 * @author 刘勇
 *
 */
public interface JudgeConditionService extends Serializable {

	/**
	 * 
	 * 要素条件判断
	 * 
	 * @param bka584
	 *            要素
	 * @param bka592
	 *            关联关系(统一小写)
	 * @param bka596
	 *            数据类型(统一大写)
	 * @param bka591
	 *            关联的政策值
	 * @param factorValue
	 *            要素实际值
	 * @return 成立true 不成立false
	 */
	public boolean judgeCondition(String bka584, String bka592, String bka596, String bka591, String factorValue);

}
