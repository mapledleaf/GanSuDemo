package com.powersi.biz.medicare.yygh.service.api.mccc;

import com.powersi.biz.medicare.diagnose.pojo.DiagnoseInfoDTO;

/**
 * 
 * 判断是否移动支付业务 医院调用中心端通知银海退费
 *
 */
public interface MCCCbizc110108Service {

	/**
	 * 判断是否移动支付成功的业务 是移动支付成功的业务，通过中心调用银海退费接口，通知银海退费 失败返回0，成功返回1
	 * 
	 * @param diagnoseInfoDTO
	 * @return
	 */
	public int toRefund(DiagnoseInfoDTO diagnoseInfoDTO);

}
