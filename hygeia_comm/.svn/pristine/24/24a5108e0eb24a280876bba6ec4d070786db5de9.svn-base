package com.powersi.biz.medicare.yygh.service.api.mccc;

import java.util.Map;

import com.powersi.biz.medicare.yygh.pojo.MobilePayDTO;

/**
 * 医院调用中心端，中心端调用银海接口，查询对账单
 */
public interface MCCCbizc410006Service {

	/**
	 * 医院调用中心端，中心端调用银海接口，查询对账单
	 * 
	 * @param mobilePayDTO
	 * @return
	 */
	@SuppressWarnings("rawtypes")
	public Map unifiedBillDownload(MobilePayDTO mobilePayDTO);

}
